apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cumulusbankaccsvc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "cumulusbankaccsvc.name" . }}
    helm.sh/chart: {{ include "cumulusbankaccsvc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cumulusbankaccsvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
